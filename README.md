# pi-mairinque-002
Site desenvolvido com flask através do curso feito pela Univesp para base do projeto integrador

Para instalar:
````console
py -3 -m venv .venv
pip3 install flask
pip3 install sqlalchemy
pip3 install flask-sqlalchemy
pip install -r requirements-dev.txt

````